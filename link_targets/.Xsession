#!/bin/sh

while true
do
	if [ `hostname` == "temperance" ]; then
		~/bin/LayoutUndock
	fi
	xscreensaver-command -exit
	xscreensaver -no-splash &
	killall gpg-agent
	eval `gpg-agent --daemon`
	wmii
done
